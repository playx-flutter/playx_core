name: playx_core
description: Core package for playx eco system contains shared classes and utilities.
version: 0.0.5
homepage: https://github.com/playx-flutter/core
repository: https://github.com/playx-flutter/playx_core

environment:
  sdk: ">=2.19.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.1.1
  flutter_readable: ^0.0.6
  get: ^4.6.5
  equatable: ^2.0.5
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
