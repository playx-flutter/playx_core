name: playx_core
description: Core package for playx eco system contains shared classes and utilities.
version: 1.0.0-beta.1
homepage: https://github.com/playx-flutter/playx_core
repository: https://github.com/playx-flutter/playx_core
issue_tracker: https://github.com/playx-flutter/playx_core/issues
topics:
  - playx
  - ecosystem
  - core
  - prefs
  - env

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.5
  equatable: ^2.0.7
  sprintf: ^7.0.0
  flutter_secure_storage: ^10.0.0-beta.2
  flutter_dotenv: ^5.2.1
  get_it: ^8.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
